{
  "name": "opencodex-ide",
  "displayName": "OpenCodex IDE",
  "description": "Multi-agent Cursor/VS Code sidebar for local opencodex: context, tools, pipelines, apply/patch, secure SecretStorage.",
  "version": "0.3.0",
  "publisher": "novexus-local",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/EstebanA22/opencodex-ide.git"
  },
  "bugs": {
    "url": "https://github.com/EstebanA22/opencodex-ide/issues"
  },
  "homepage": "https://github.com/EstebanA22/opencodex-ide#readme",
  "engines": {
    "vscode": "^1.85.0"
  },
  "categories": [
    "AI",
    "Chat",
    "Other"
  ],
  "keywords": [
    "opencodex",
    "codex",
    "multi-agent",
    "cursor",
    "proxy",
    "security"
  ],
  "activationEvents": [
    "onStartupFinished"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "viewsContainers": {
      "activitybar": [
        {
          "id": "opencodex",
          "title": "OpenCodex",
          "icon": "media/opencodex.svg"
        }
      ]
    },
    "views": {
      "opencodex": [
        {
          "type": "webview",
          "id": "opencodex.chatView",
          "name": "Multi-Agent"
        }
      ]
    },
    "commands": [
      {
        "command": "opencodex.openDashboard",
        "title": "OpenCodex: Open Dashboard",
        "icon": "$(globe)"
      },
      {
        "command": "opencodex.refreshHealth",
        "title": "OpenCodex: Refresh Health",
        "icon": "$(refresh)"
      },
      {
        "command": "opencodex.focusChat",
        "title": "OpenCodex: Focus Chat"
      },
      {
        "command": "opencodex.sendSelectionToAgent",
        "title": "OpenCodex: Send Selection To Agent"
      },
      {
        "command": "opencodex.setApiKey",
        "title": "OpenCodex: Set API Key (SecretStorage)"
      },
      {
        "command": "opencodex.clearApiKey",
        "title": "OpenCodex: Clear API Key"
      }
    ],
    "menus": {
      "view/title": [
        {
          "command": "opencodex.openDashboard",
          "when": "view == opencodex.chatView",
          "group": "navigation"
        },
        {
          "command": "opencodex.refreshHealth",
          "when": "view == opencodex.chatView",
          "group": "navigation"
        }
      ],
      "editor/context": [
        {
          "command": "opencodex.sendSelectionToAgent",
          "when": "editorHasSelection",
          "group": "opencodex@1"
        }
      ]
    },
    "configuration": {
      "title": "OpenCodex IDE",
      "properties": {
        "opencodex.baseUrl": {
          "type": "string",
          "default": "http://127.0.0.1:10100/v1",
          "description": "OpenAI-compatible base URL for the local opencodex proxy."
        },
        "opencodex.apiKey": {
          "type": "string",
          "default": "dummy",
          "description": "Fallback only. Prefer OpenCodex: Set API Key (SecretStorage). Do not put real secrets here."
        },
        "opencodex.defaultModel": {
          "type": "string",
          "default": "gpt-5.6-sol",
          "description": "Default model id from the proxy catalog."
        },
        "opencodex.failoverModels": {
          "type": "array",
          "items": { "type": "string" },
          "default": ["gpt-5.6-terra", "gpt-5.5"],
          "description": "Models to try if the primary returns 429/502/503/504."
        },
        "opencodex.enableToolsDefault": {
          "type": "boolean",
          "default": false,
          "description": "Default for the Tools checkbox (terminal/tests require approval)."
        }
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "package": "vsce package --no-dependencies",
    "test:smoke": "node ./scripts/smoke-test.mjs",
    "test:security": "node ./scripts/security-check.mjs"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "@types/vscode": "^1.85.0",
    "@vscode/vsce": "^3.2.0",
    "typescript": "^5.4.0"
  }
}
